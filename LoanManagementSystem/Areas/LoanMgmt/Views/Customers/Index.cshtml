@using LoanManagementSystem.Models.Enums
@using Microsoft.AspNetCore.Identity

@model IEnumerable<LoanManagementSystem.Models.Customer>

@inject UserManager<IdentityUser> _userManager

@{
    ViewData["Title"] = "Index";
      var user = await _userManager.GetUserAsync(this.User);
    var isAdmin = await _userManager.IsInRoleAsync(user, MyIdentityRoleNames.LoanAdmin.ToString());
}

    <style>

        body{

        background-color:aquamarine;
        background-repeat:no-repeat;
        background-attachment:fixed;
        background-size:100%;

        }
    </style>
  @if (isAdmin)
{
    <center><h1><b>List of Customers</b></h1></center>

    @*<p>

    @if (isAdmin)
    {
        <a class="btn btn-primary disabled"  href="#">Create New</a>

    }
    else {
        <a class="btn btn-primary "asp-action="Create">Create New</a>

    }         
</p>
*@


    @*
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FullName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Address)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Phonenumber)
            </th>
              <th>
                @Html.DisplayNameFor(model => model.BirthDate)

            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>

            <th>
                @Html.DisplayNameFor(model => model.Age)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Gender)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.State)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Plan) 
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FullName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Address)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Phonenumber)
            </td>
             <td>
                @Html.DisplayFor(modelItem => item.BirthDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>

            <td>
                @Html.DisplayFor(modelItem => item.Age)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Gender)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.State)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Plan.PlanType) 
            </td>
            <td>
              @if (isAdmin)
              {
                <a class="btn btn-sm btn-primary disabled" href="#" >Edit</a> 
                 <a class="btn btn-sm btn-primary disabled" href="#">Delete</a>
              }
                else {
                 <a class="btn btn-sm btn-primary" asp-action="Edit" asp-route-id="@item.CustomerId">Edit</a>
                 <a class="btn btn-sm btn-primary" asp-action="Edit" asp-route-id="@item.CustomerId">Delete</a>
                }   
                <a  class="btn btn-sm btn-primary"  asp-action="Details" asp-route-id="@item.CustomerId">Details</a> 


            </td>
        </tr>                
}                   
    </tbody>
</table>*/*@
    <div class="card-columns">
        @foreach (var item in Model)
        {
            <div class="card">
                <div class="card-header">
                    @Html.DisplayNameFor(modelItem => item.FullName): @item.FullName
                </div>
                <div class="card-body">
                    @Html.DisplayNameFor(modelItem => item.Plan.PlanType): @item.Plan.PlanType
                    <br/>
                    @Html.DisplayNameFor(modelItem => item.Email): @item.Email
                    <br/>
                    @Html.DisplayNameFor(modelItem => item.Phonenumber): @item.Phonenumber

                    <br/>
                    @Html.DisplayNameFor(modelItem => item.BirthDate): @item.BirthDate

                    <br/>
                    @Html.DisplayNameFor(modelItem => item.Age):-@item.Age
                    <br/>
                    @Html.DisplayNameFor(modelItem => item.Gender):  @item.Gender
                    <br/>       
                    @Html.DisplayNameFor(modelItem => item.Address): @item.Address
                    <br/>
                    @Html.DisplayNameFor(modelItem => item.State): @item.State

                </div>
                <div class="card-footer">

                    @if (isAdmin)
                    {
                        @* <a class="btn btn-primary disabled" href="#" >Edit</a> 
                            <a class="btn btn-primary disabled" href="#" >Delete</a>*@
                        <a class="btn btn-warning" asp-area="LoanMgmt" asp-controller="Approvals" 
                       asp-action="Create" asp-route-id="@item.CustomerId">Loan Status</a>
                    }
                    else
                    {
                        <a class="btn btn-primary" asp-action="Edit" asp-route-id="@item.CustomerId">Edit</a>
                        <a class="btn btn-primary" asp-action="Delete" asp-route-id="@item.CustomerId">Delete</a>
                    }   
                    <a  class="btn btn-dark"  asp-action="Details" asp-route-id="@item.CustomerId">Details</a> 

                </div>
            </div>
        }
    </div>
}
else
{
    <h1 style="text-align:center"> Thank You For Applying</h1>
     <h1 style="text-align:center"> Submit Successfully</h1>
}
<a class="btn btn-sm btn-warning btn-outline-danger" asp-area="LoanMgmt" asp-controller="Plans" asp-action="Index">List of Loan Plans</a>
 
  <a class="btn btn-sm btn-danger text-white" asp-area="" asp-page="/Index">Back To Home</a>

